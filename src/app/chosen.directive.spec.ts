import { ChosenDirective } from './chosen.directive';

describe('ChosenDirective', () => {
  it('should create an instance', () => {
    const directive = new ChosenDirective();
    expect(directive).toBeTruthy();
  });
});
